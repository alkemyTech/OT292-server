import { expect } from 'chai';

describe('Borrar este archivo una vez que se agregen mas test', () => {
  it('true=true', () => {
    // eslint-disable-next-line @typescript-eslint/no-unused-expressions
    expect(true).to.be.true;
  });
});
